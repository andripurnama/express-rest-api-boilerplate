import dotenv from "dotenv";
dotenv.config()
describe('user repository tests', () => {
    it('should user repository class exists', () => {
        const userRepository = new UserRepository();
        
    })
})